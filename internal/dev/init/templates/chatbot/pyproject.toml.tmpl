[project]
name = "{{.ProjectName}}"
version = "0.1.0"
description = "A duragraph chatbot project."
requires-python = ">=3.11"
dependencies = [
    "duragraph>=0.7.0",
    "openai>=1.40.0",
]

[tool.uv]
package = false
