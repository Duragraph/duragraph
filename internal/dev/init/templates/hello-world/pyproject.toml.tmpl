[project]
name = "{{.ProjectName}}"
version = "0.1.0"
description = "A duragraph project scaffolded from the hello-world template."
requires-python = ">=3.11"
dependencies = [
    "duragraph>=0.7.0",
]

[tool.uv]
package = false
