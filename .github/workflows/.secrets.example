# Example secrets file for Act
# Copy this file to .secrets and fill in your actual values
# Act will load secrets from this file when running locally

# NEVER commit the actual .secrets file to git!

# OpenAI API Key (optional, for testing LLM integration)
OPENAI_API_KEY=sk-...

# Anthropic API Key (optional, for testing LLM integration)
ANTHROPIC_API_KEY=sk-...

# GitHub Token (for accessing private repos, creating releases, etc.)
GITHUB_TOKEN=ghp_...

# Docker Hub credentials (for pushing images)
DOCKER_USERNAME=your-username
DOCKER_PASSWORD=your-password

# Cloud provider tokens (if testing deployment workflows)
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
DIGITALOCEAN_TOKEN=...
PULUMI_ACCESS_TOKEN=...

# Database credentials (for integration tests)
DB_PASSWORD=test-password
JWT_SECRET=test-secret-do-not-use-in-production

# Cloudflare Pages (for docs deployment to Cloudflare)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# GitHub Pages deployment
# Note: GITHUB_TOKEN is automatically provided by GitHub Actions
# For local testing with Act, use a personal access token with 'repo' scope
