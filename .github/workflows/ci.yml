name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  pre-commit:
    name: Pre-commit Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Install Go tools
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1

  unit_go:
    name: Go Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Go Vet
        run: go vet ./...

      - name: Go Unit Tests
        run: go test ./... -cover -short

  # Docs have been migrated to duragraph-docs repository
  # build_docs:
  #   name: Build Docs
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #
  #     - name: Install pnpm
  #       uses: pnpm/action-setup@v4
  #       with:
  #         version: 9
  #
  #     - name: Install dependencies
  #       working-directory: docs
  #       run: pnpm install
  #
  #     - name: Build docs
  #       working-directory: docs
  #       run: pnpm build
  #
  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: docs-site
  #         path: docs/dist/
