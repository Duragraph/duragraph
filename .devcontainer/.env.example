# Example environment variables for devcontainer
# Copy this to ~/.devcontainer.env on your HOST machine and fill in your values
# DO NOT commit the actual .env file with real values!

# Git configuration
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com

# GPG configuration (optional - for commit signing)
# To get your GPG key ID: gpg --list-secret-keys --keyid-format=long
# To export private key: gpg --export-secret-keys YOUR_KEY_ID | base64
GPG_KEY_ID=YOUR_GPG_KEY_ID_HERE
GPG_PRIVATE_KEY=YOUR_BASE64_ENCODED_PRIVATE_KEY_HERE
